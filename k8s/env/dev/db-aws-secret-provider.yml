apiVersion: secrets-store.csi.x-k8s.io/v1
kind: SecretProviderClass
metadata:
  name: db-aws-secrets
  namespace: dev
spec:
  provider: aws
  parameters:
    objects: |
      - objectName: "rds!db-cf0615c3-91af-4a2f-b3d0-892d7d53c1c4" 
        objectType: "secretsmanager"
        jmesPath:
          - path: username
            objectAlias: username
          - path: password
            objectAlias: password
